export const loginSuccess = (data) =>{
    return{
        type: "LOGIN_SUCCESS",
        payload : data
    }
}
export const products = (data) =>{
    return{
        type: "LOGIN_SUCCESS",
        payload : data
    }
}